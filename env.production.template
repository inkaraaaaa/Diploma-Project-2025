# Database Configuration
POSTGRES_DB=intern_go
POSTGRES_USER=admin
POSTGRES_PASSWORD=change_this_secure_password

# Django Configuration
DJANGO_SECRET_KEY=change_this_very_long_secret_key_for_production
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=your-domain.com,your-ip-address,localhost

# Redis Configuration (for caching and sessions)
REDIS_PASSWORD=change_this_redis_password

# Email Configuration (Optional - update with your SMTP settings)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# SSL Configuration
SSL_REDIRECT=True

# Backup Configuration
BACKUP_LOCATION=/backups 